import { TransfromInterceptor } from './transform.interceptor';

describe('TransfromInterceptor', () => {
  it('should be defined', () => {
    expect(new TransfromInterceptor()).toBeDefined();
  });
});
